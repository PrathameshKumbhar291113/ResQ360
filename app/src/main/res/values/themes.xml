<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ResQ360" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.ResQ360" parent="Base.Theme.ResQ360" />

    <style name="CardView." parent="CardView">
        <item name="android:foreground">@drawable/shape_card_gradient</item>
    </style>
</resources>